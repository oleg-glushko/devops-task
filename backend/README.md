# Backend example in Flask

By default it starts listening on the port 5000, to change use a `FLASK_PORT` environment variable. And don't forget to reflect this adjustment on the frontend counterpart.
